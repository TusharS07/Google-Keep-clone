import { FilterNotesForSearchPipe } from './filter-notes-for-search.pipe';

describe('FilterNotesForSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNotesForSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
